import 'package:flutter/material.dart';

const Color kMaleActiveColor = Colors.blueAccent;  //It's a convention to define constants starting with k;
const Color kFemaleActiveColor = Colors.pinkAccent;
const Color kInactiveColor = Color(0xFF1D1E33);
const Color kButtonColor = Colors.pink;

const TextStyle kLabelTextStyle = TextStyle(fontSize: 20, color: Colors.white70);
const TextStyle kNumberTextStyle = TextStyle(fontWeight: FontWeight.w900, fontSize: 50);